_schema-version: "3.2"
ID: prodsugmanageproducts2
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: prodsugmanageproducts2-destination-content
  type: com.sap.application.content
  requires:
  - name: prodsugmanageproducts2-destination-service
    parameters:
      content-target: true
  - name: prodsugmanageproducts2-repo-host
    parameters:
      service-key:
        name: prodsugmanageproducts2-repo-host-key
  - name: prodsugmanageproducts2-uaa
    parameters:
      service-key:
        name: prodsugmanageproducts2-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: prodsugmanageproducts2_html_repo_host
          ServiceInstanceName: prodsugmanageproducts2-html5-service
          ServiceKeyName: prodsugmanageproducts2-repo-host-key
          sap.cloud.service: prodsugmanageproducts2
        - Authentication: OAuth2UserTokenExchange
          Name: prodsugmanageproducts2_uaa
          ServiceInstanceName: prodsugmanageproducts2-xsuaa-service
          ServiceKeyName: prodsugmanageproducts2-uaa-key
          sap.cloud.service: prodsugmanageproducts2
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: prodsugmanageproducts2-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: prodsugmanageproducts2-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - prodsugmanageproducts2.zip
      name: prodsugmanageproducts2
      target-path: resources/
- name: prodsugmanageproducts2
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: prodsugmanageproducts2-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: prodsugmanageproducts2-destination-service
    service-plan: lite
- name: prodsugmanageproducts2-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: prodsugmanageproducts2-xsuaa-service
    service-plan: application
- name: prodsugmanageproducts2-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: prodsugmanageproducts2-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
